###############################################
# Node MSSQL MCP Server Environment (.env.example)
# Copy to .env for local dev or configure in ACA.
###############################################

### Azure AD Service Principal (Client Credentials) for SQL access (DO NOT commit real values)
AZURE_CLIENT_ID=__your_aad_app_client_id__
AZURE_CLIENT_SECRET=__your_aad_app_client_secret__
AZURE_TENANT_ID=__your_tenant_id__

### SQL target
SERVER_NAME=__your_server_hostname__.database.windows.net
DATABASE_NAME=__your_database_name__
READONLY=true                    # Set false to enable write tools (insert/update/create/drop/index)
TRUST_SERVER_CERTIFICATE=false   # Enable only for local dev / private endpoints
CONNECTION_TIMEOUT=30            # Seconds

### HTTP bridge
HTTP_PORT=8080                   # Local port for HTTP tool gateway

### Logging / diagnostics (optional)
LOG_LEVEL=info
DEBUG_STARTUP=false              # Set true for verbose startup + env redaction logs

### API Key auth (required in production). Provide ONE of MCP_API_KEY or list in MCP_API_KEYS
MCP_API_KEY=__generate_a_long_random_key__
# MCP_API_KEYS=key1,key2,key3

### Build metadata (optional)
GIT_COMMIT=__auto_injected_by_ci__

### Notes
# 1. Never commit a real AZURE_CLIENT_SECRET or MCP_API_KEY.
# 2. For Azure Container Apps, configure these as secrets / environment variables in the revision template.
# 3. Rotate MCP_API_KEY immediately if exposed.
